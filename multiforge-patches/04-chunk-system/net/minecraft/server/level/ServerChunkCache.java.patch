--- a/net/minecraft/server/level/ServerChunkCache.java
+++ b/net/minecraft/server/level/ServerChunkCache.java
@@ -139,6 +139,7 @@
         } else {
             ProfilerFiller profilerfiller = this.level.getProfiler();
             profilerfiller.incrementCounter("getChunk");
+            net.multiforge.neoforge.chunk.ServerChunkCacheBridge.observeGet(this, p_8360_, p_8361_, p_330876_, p_8363_);
             long i = ChunkPos.asLong(p_8360_, p_8361_);

             for (int j = 0; j < 4; j++) {
@@ -299,10 +299,12 @@
     public void save(boolean p_8420_) {
         this.runDistanceManagerUpdates();
         this.chunkMap.saveAllChunks(p_8420_);
+        net.multiforge.neoforge.chunk.ServerChunkCacheBridge.afterSave(this, p_8420_);
     }

     @Override
     public void close() throws IOException {
+        net.multiforge.neoforge.chunk.ServerChunkCacheBridge.beforeClose(this);
         this.save(true);
         this.lightEngine.close();
         this.chunkMap.close();
@@ -326,6 +326,7 @@
         this.chunkMap.tick(p_201913_);
         this.level.getProfiler().pop();
         this.clearCache();
+        net.multiforge.neoforge.chunk.ServerChunkCacheBridge.afterTickChunks(this, p_201913_);
     }

     private void tickChunks() {
